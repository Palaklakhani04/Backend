const add = (a,b) => {
    return a + b;
}

const multi = (a,b) => {
    return a * b;
}

const sub = (a,b) => {
    return a - b;
}

// module.exports.add = add
// module.exports.multi = multi
// module.exports.sub = sub

module.exports = {add, multi, sub}